# Copy to .env and fill in whichever provider you use.
# AquaMind runs fully without any key — it falls back to DemoChatModel.
OPENAI_API_KEY=
GOOGLE_API_KEY=
AQUAMIND_MODEL=demo          # demo | openai | gemini
